import 'package:flutter/material.dart';
import '../widgets/start_journey_body.dart';

class StartJourneyView extends StatelessWidget {
  const StartJourneyView({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(body: StartJourneyBody());
  }
}
